export enum Dirname {
  SRC = 'src',
  COMPONENT = `src/components`,
  SVG = 'icons',
  STORY = 'stories',
}

export enum Filename {
  TREE = 'tree.ts',
}

const QIConfig = {

};

export default QIConfig;
