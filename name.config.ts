import * as path from "path";

export const Dirname = {
  SRC: 'src',
  COMPONENT: path.join(`src`,`components`),
  SVG: 'icons',
  STORY: 'stories',
}

export const Filename = {
  TREE: 'tree.ts',
}
